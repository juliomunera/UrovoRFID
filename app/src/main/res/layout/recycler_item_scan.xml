<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:gravity="center_vertical">

        <!-- EPC / Descripción del inventario -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="3"
            android:orientation="vertical">

            <!--
                Si el TAG está en el inventario → muestra la descripción (azul índigo)
                Si no está en el inventario     → muestra el EPC raw (monospace, gris)
            -->
            <TextView
                android:id="@+id/tv_epc"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#424242"
                android:textSize="12sp"
                android:fontFamily="monospace"
                android:text="EPC" />

            <!--
                EPC pequeño debajo de la descripción.
                Solo visible cuando el TAG tiene descripción en el inventario.
            -->
            <TextView
                android:id="@+id/tv_sub_epc"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#9E9E9E"
                android:textSize="10sp"
                android:fontFamily="monospace"
                android:visibility="gone"
                android:text="" />

        </LinearLayout>

        <!-- RSSI -->
        <TextView
            android:id="@+id/tv_rssi"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="#1565C0"
            android:textSize="12sp"
            android:text="-dBm" />

        <!-- Contador -->
        <TextView
            android:id="@+id/tv_count"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:textColor="#757575"
            android:textSize="12sp"
            android:text="1" />

    </LinearLayout>

    <!-- Divisor -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#E0E0E0" />

</LinearLayout>
